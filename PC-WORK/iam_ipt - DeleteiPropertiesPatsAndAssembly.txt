'Setup iProperties to nothing

iProperties.Value("Custom", "Description") = ""

'Part of code for delete custom properties
'------------------------------------------------------------------------------
'Define list of custom properties to delete
'For add a new custom properties, just make the next steps

'MyArraysList.Add("Name of new iProperties for delete") 

Dim MyArrayList As New ArrayList
MyArrayList.Add("N° Révision") 'for parts
MyArrayList.Add("N° Révision ")' for assembly 
MyArrayList.Add("Description")' for assembly 

'MyArrayList.Add("Next example")

'Define custom property collection
oCustomPropertySet = ThisDoc.Document.PropertySets.Item("Inventor User Defined Properties")
'Look at each property in the collection
For Each oCustProp In oCustomPropertySet
	'Check property name against the list you want to delete
	If MyArrayList.Contains(oCustProp.name)Then
	'delete the custom iProperty
	oCustProp.Delete 
	Else 
'skip it
	End If
Next

'------------------------------------------------------------------------------
